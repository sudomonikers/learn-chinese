type HSKLevel @model @auth(rules: [{allow: public}]) {
  id: ID!
  level: String
}

type ChineseWord @model @auth(rules: [{allow: public}]) {
  id: ID!
  character: String!
  pinyin: String!
  sound: AWSURL!
  HSKLevel: HSKLevel @hasOne
}
 